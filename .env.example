# Example Environment Variables
# Copy this file to .env.local for local development overrides

# Backend API URL (optional)
#
# Leave unset to use defaults:
#   - Development (npm run dev): http://localhost:5044/api
#   - Production (npm run build): /api (relative path, same server)
#
# Set VITE_API_URL only if you need to override the default:
#   - To use production backend while developing: https://your-deployed-backend.com/api
#   - To deploy frontend separately from backend: https://your-backend-server.com/api
#
# VITE_API_URL=http://localhost:5044/api
